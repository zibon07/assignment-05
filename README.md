What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
ans:by getElementById i can get a single element with a specific id, by getElementsByclassName i can get all elements with specific class name!by querySelector i can get the first element with matching css selectors,with querySelectorAll i can get all elements with matching css selector!
How do you create and insert a new element into the DOM?
ans:i create a new element using document.createElemen() then insert it into the dom using append() or appendChild()!
What is Event Bubbling and how does it work?
ans: event bubbling means when you click something the event happens on that elements first then moves up to the parent element one by one !
What is Event Delegation in JavaScript? Why is it useful?
ans:event delegation is a technique where you add one event listener to a parent element instead of adding listeners to many child elements , it is usefull because one parent handles events for all its children!
What is the difference between preventDefault() and stopPropagation() methods?
ans:preventDefault() stops what the browser does, stopPropaagation() stops where the event goes !
